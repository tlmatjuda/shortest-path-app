

# ======================================================================================================================
# CUSTOM CONFIGS
# ======================================================================================================================
spp:
    app:
        conf:
            file:
                support-data: classpath:/data/support-data.xlsx



# ======================================================================================================================
# SPRING CONFIGS
# ======================================================================================================================
spring:
    datasource:
        url: jdbc:tc:postgresql:16-alpine3.19:///test_database
        username: user
        password: password
    flyway:
        enabled: false
    jpa:
        hibernate:
            ddl-auto: create